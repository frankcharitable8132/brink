## What

<!-- one or two sentences -->

## Why

<!-- the problem, or the issue number -->

## Checked

- [ ] `./build.sh` passes on macOS (or `dotnet build windows/Brink.csproj -c Release` on Windows)
- [ ] Tokens still go only to the provider's own API host; nothing new is stored
- [ ] If it touches the UI: screenshot / GIF below
- [ ] If it's user-visible: line added under `## [Unreleased]` in `CHANGELOG.md`
- [ ] New strings added to **all** `Resources/*.lproj/Localizable.strings` (English is fine for the others; mark with `// TODO translate`)
